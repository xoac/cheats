% cargo fmt,

# Format code in documentation
files=($(echo "<files>"))
cargo fmt -- --config format_code_in_doc_comments=true -- "${files[@]}"

$ files: echo "*" && find . -iname '*.rs' -type f -print  --- --multi
